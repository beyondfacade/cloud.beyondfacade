# 재개 메모 — 2026-09-23 18:10 중단

## 바로 다음에 할 일

**파생 지표 구현.** 설계서와 규칙이 다 준비돼 있어 에이전트에 그대로 넘기면 된다.

읽을 것 순서대로:
1. `docs/superpowers/specs/2026-09-23-region-profile-design.md` — 이 작업의 설계서
2. `docs/superpowers/specs/2026-09-23-neighborhood-typology.md` — 판정 규칙 원본 (§4-1 결정 목록, §6 시간대 라벨, §7 구현 지침)
3. `backend/apps/metric/` — 확장 대상
4. `backend/docs/backend_ver_log.md` v0.25.0 — 마이그레이션 head 처리 방식

만들 것은 `apps/metric` 확장, 백엔드 **v0.26.0**, 테이블 둘.

- `region_profile_quarter` — 행정동 × 분기, 약 9,350행
- `region_industry_hour_gap_quarter` — 행정동 × 업종 × 분기 × 시간구간, 약 46만행

새 마이그레이션의 부모는 현재 브랜치 head `f3c9a1d47b62`다.

18:09에 에이전트를 중단했고 **소스는 한 줄도 안 건드린 상태**다. 작업 트리가 깨끗하니 처음부터 다시 돌리면 된다.

## 절대 틀리면 안 되는 것 셋

**① 시간대는 시간당 강도로 보정한다.** 6구간 길이가 다르다(6·5·3·3·4·3시간). 원값으로 비교하면 425개 동 중 377개가 `00_06` 최대로 나온다. 유동인구와 매출 분해 양쪽에 똑같이 적용한다.

```
시간강도 = 구간값 ÷ 구간길이 ÷ (전체값 ÷ 24)
```

**② 임계값 하드코딩 금지.** 전부 해당 분기 창의 425개 동 분포에서 매 배치 재계산한다. 분류 문서의 수치(1.491 등)는 확인용이지 상수가 아니다.

**③ 부호 검증을 반드시 한다.** 업무 밀집형 동의 카페에서 `06_11` gap이 양수여야 한다(출근길 매출). 주거형 동에서는 `17_21` 이후가 양수여야 한다. 반대로 나오면 강도 보정이나 뺄셈 방향이 틀린 것이다.

## 오늘 끝난 것

| 커밋 | 내용 |
|---|---|
| `0c69231` | 백엔드 v0.23.0 — 추정매출·점포 104만 행 |
| `9da072b` | 프론트엔드 v0.14.1 — E2E·스크린샷 스크립트 복원·수정 |
| `22802d2` | 백엔드 v0.24.0+v0.25.0 — 매출 분해 789만 + 동네 맥락 105만 행 |
| `1c66170` | 유형 분류 규칙 + 파생 지표 설계서 |

**적재 총 999만 행, 전부 멱등.** 테스트 247 통과 / 1 실패(기존 알려진 것).

넷 다 아직 원격에 없다. 푸시할지는 확인 필요.

## 알아낸 것 중 후속에 영향이 큰 것

**점포 수와 매출은 관계가 없다.** 분기효과 제거 후 동 내부 상관이 0이다. 지도에 매출 지표를 추가하는 계획은 폐기했다. 대신 매출을 상권 묘사 재료로 쓰는 쪽으로 방향을 틀었다.

**축마다 신뢰도가 다르다.**

| 축 | 커버리지 | 사용 |
|---|---|---|
| 매출 요일·시간대 | 완전 분할 | 절대 금액 가능 |
| 매출 성별·연령 | 금액 89.2% | 구성비만. 유동인구(100%)와 나란히 놓지 말 것 |
| 유동인구 전 축 | 완전 분할 | 자유롭게 |
| 직장·상주·지출 | 완전 분할 | 자유롭게 |
| 집객시설 | total ≠ 19종 합 | 나란히 놓지 말 것 |

**지출은 주민이 쓴 돈이 아니라 KB카드 가맹점 매출이다.** 발생지 기준이고 추정매출은 신한카드 기반이라 둘 다 발생지다. 유입·유출 지수는 폐기했다. 구성비는 "이 동 가맹점 결제의 구성"으로 서술하면 쓸 수 있으나 본사·온라인 가맹점 필터가 필요하다(상위 5개 동이 서울 전체의 61%).

## 남은 태스크

| # | 태스크 | 상태 |
|---|---|---|
| T2 | 파생 지표 배치 | **다음에 할 것** |
| T3 | 사이드패널 동네 프로필 (라우터 포함) | 대기 |
| T4 | AI 리포트 시장 분석 절 슬롯 고정 | 대기 |
| T5 | 지도에 평균 영업 지속 개월 지표 추가 | 대기 |
| T6 | 부채 정리 | 대기 |

T3을 T4보다 먼저 하는 이유는 파생 지표가 쓸 만한지 화면에 띄워봐야 알기 때문이다. 사이드패널이 손이 가장 적고 결과가 즉시 보인다.

## 부채

- **`docs/erd.md` §6 갱신** — 새 테이블 11개가 반영 안 됐다.
- **alembic head 둘** — 공용 DB가 미병합 `feature/analysis-api`의 마이그레이션으로 올라가 있다. 병합 시 `alembic merge` 필요.
- **프론트엔드 랜딩 25개 파일 미커밋** — 이번 세션에서 만든 게 아니라 손대지 않았다. `frontend/docs/frontend_ver_log.md`의 v0.14.0 항목이 그 코드와 함께 커밋되기를 기다린다.
- **지출 항목 구성비 지표** — 본사·온라인 가맹점 필터가 필요해 별도 작업으로 미뤘다.

## 참고 문서

| 문서 | 내용 |
|---|---|
| `docs/superpowers/specs/2026-09-23-commerce-bc-design.md` | 매출·점포·분해 설계, 업종 매핑 확정 |
| `docs/superpowers/specs/2026-09-23-commerce-crossvalidation.md` | 점포 수 대 매출 관계 검증, 모집단 괴리 |
| `docs/superpowers/specs/2026-09-23-neighborhood-bc-design.md` | 동네 맥락 8테이블, 원천 함정 |
| `docs/superpowers/specs/2026-09-23-neighborhood-typology.md` | 유형 분류 규칙 원본 |
| `docs/superpowers/specs/2026-09-23-region-profile-design.md` | 파생 지표 설계 |
| `docs/superpowers/specs/2026-09-23-spending-definition.md` | 지출 정의 판정 |
| `HANDOFF-260918.md` §2-1·§4-1 | 데이터 확보 경로, 캠퍼스 반출 정책 |
| `docs/후보.md` | 외부 참고 후보 (상권 프로필 아이디어의 출처) |

원본 데이터는 `data/raw/seoul_commerce/`에 있고 `MANIFEST.md`에 무결성과 재현용 명령이 있다. 루트 `data/`는 git 무시 대상이다.

"""동네 맥락 공통 구간 어휘 — 여러 엔티티가 횡단 공유하는 값 객체 (§12 AOP 예외 · 설계서 §5).

`commerce` BC의 추정매출 분해와 **값이 완전히 같다.** 원천 표기만 다르다 — 이쪽 시간대는
밑줄(`시간대_00_06_유동인구_수`), 저쪽은 물결표(`시간대_00~06_매출_금액`)다. 저장하는 dim_key는
양쪽 모두 `00_06`으로 정규화해, 나중에 두 BC를 조인할 때 구분자 차이로 어긋나지 않게 한다.

BC를 가로지르는 중복을 감수하고 각자 갖는다 — 값 객체는 도메인이라 전역 인프라 `core/`에 둘 수
없고(§11), BC 독립성이 중복 제거보다 우선이다(설계서 §5).
"""

# 원천 `연령대_10` ~ `연령대_60_이상` 6구간
AGE_KEYS: tuple[str, ...] = ("10", "20", "30", "40", "50", "60_over")

# 원천 `시간대_00_06` ~ `시간대_21_24` 6구간
HOUR_KEYS: tuple[str, ...] = ("00_06", "06_11", "11_14", "14_17", "17_21", "21_24")

# 원천 `월요일` ~ `일요일` 7종
DOW_KEYS: tuple[str, ...] = ("mon", "tue", "wed", "thu", "fri", "sat", "sun")

GENDER_KEYS: tuple[str, ...] = ("male", "female")
